<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="#333333"
    tools:context=".NewList" >

    <EditText
        android:id="@+id/NewListEditText"
        android:inputType="text"
        android:hint="Type list name"
        android:textColor="#FFFFFF"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <Button
        android:id="@+id/NewListOKButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@id/NewListEditText"
        android:layout_below="@id/NewListEditText"
        android:layout_marginTop="38dp"
        android:text="OK" />
    
        <Button
        android:id="@+id/NewListCancelButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/NewListOKButton"
        android:layout_below="@id/NewListEditText"
        android:layout_marginTop="38dp"
        android:text="Cancel" />

</RelativeLayout>